# Website Placeholder

The House of Tartarus website platform has not yet been selected.

Possible directions:

1. Static HTML/CSS/JavaScript
   - Source belongs in `static-html/`.
   - Shared media belongs in `shared/assets/`.

2. WordPress
   - Custom theme, child theme, plugin, or deployment documentation belongs in `wordpress/`.
   - Shared media belongs in `shared/assets/`.

Keep branding files platform-neutral so the same assets can be used by either implementation.
