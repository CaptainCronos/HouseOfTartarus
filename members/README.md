# Member Cards

Use one directory per member or card identity.

Example:

```text
members/
└── CaptainCronos/
    ├── member.conf
    ├── front.png
    ├── back.png
    └── proof.pdf
```

Do not store private information that is not intended to appear on the finished card.
